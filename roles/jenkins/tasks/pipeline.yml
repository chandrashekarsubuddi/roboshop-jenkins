- name: Create Config file
  ansible.builtin.template:
    src: job.xml
    dest: /tmp/job.xml


- name: Create Job - "{{item}}"
  community.general.jenkins_job:
    config: "{{ lookup('file', '/tmp/job.xml') }}"
    name: "{{item}}"
    url: http://172.31.10.56:8080
    user: admin
    password: "{{jenkins_password}}"


